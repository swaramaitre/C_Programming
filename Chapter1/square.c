//Program to calculate area of square(side is given)
#include<stdio.h>
int main()
{
    int side;
    printf("Enter side of sqaure: ");
    scanf("%d", &side);

    printf("The area of square is: %d", side*side);
    return 0;
}